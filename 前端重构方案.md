# 前端重构方案：迁移至 Vue Vben Admin

## 1. 目标与背景

将现有的基于 `gin-vue-admin` (Arco Design) 的前端项目，重构迁移至 **[Vue Vben Admin](https://github.com/vbenjs/vue-vben-admin)** 框架。

*   **目标框架**：Vue Vben Admin (最新版 5.x)
*   **UI 组件库**：Ant Design Vue
*   **编程语言**：TypeScript
*   **关键约束**：`@novnc/novnc` 依赖必须锁定在 **1.4.0** 版本（高版本存在构建兼容性问题）。

---

## 2. 核心技术栈对比

| 模块 | 原项目 (Current) | 新项目 (Target) | 迁移策略 |
| :--- | :--- | :--- | :--- |
| **语言** | JavaScript | **TypeScript** | 需重写逻辑，补充类型定义 (Interface/Type) |
| **UI 库** | Arco Design Vue | **Ant Design Vue** | 组件属性不兼容，需逐个页面重写 Template |
| **构建工具** | Vite | Vite | 沿用，配置需适配 Vben 插件链 |
| **包管理** | npm | **pnpm** (推荐) | Vben 强制使用 monorepo 结构，推荐 pnpm |
| **网络请求** | Axios (基础封装) | Axios (Vben 深度封装) | 适配 `defHttp` 调用方式，重构拦截器 |
| **状态管理** | Pinia | Pinia | 适配 Vben 的 Store 结构 |

---

## 3. 详细迁移步骤 (Step-by-Step)

建议采用 **平行重构** 策略，即新建 `front-end-vben` 目录，逐步迁移功能，而不修改原项目代码。

### 第一阶段：环境搭建与初始化

1.  **创建项目**
    ```bash
    # 在项目根目录执行
    pnpm create vben@latest
    # 选择: Ant Design Vue 版本
    ```
2.  **清理无用模块**
    *   Vben 默认包含大量演示代码，需按需移除 `src/views/demo` 等目录。
3.  **安装关键依赖**
    *   **特别注意**：安装 noVNC 时必须指定版本。
    ```bash
    pnpm add @novnc/novnc@1.4.0
    ```
    *   在 `package.json` 中确认版本号没有 `^` 前缀，强制锁定：
    ```json
    "@novnc/novnc": "1.4.0"
    ```
4.  **配置代理 (Proxy)**
    *   修改 `.env.development` 或 `vite.config.ts`，指向 Django 后端：
    ```typescript
    // vite.config.ts
    server: {
      proxy: {
        '/api': {
          target: 'http://127.0.0.1:8000',
          changeOrigin: true,
          ws: true, // 开启 WebSocket 支持 (noVNC 需要)
        },
      },
    }
    ```

### 第二阶段：基础服务对接

1.  **API transform (JS -> TS)**
    *   将 `src/api/*.js` 中的接口迁移到新项目 `src/api/*/*.ts`。
    *   **重点**：为每个接口定义 `Result` 类型，例如：
    ```typescript
    // src/api/pve/model/vmModel.ts
    export interface VmInfo {
      vmid: number;
      name: string;
      status: string;
      // ...
    }
    ```
2.  **登录鉴权 (Auth)**
    *   修改 `src/store/modules/user.ts`。
    *   对接 Django 的 `/api/v1/login/` 接口。
    *   配置 JWT Token 的存储与过期刷新逻辑。

### 第三阶段：核心业务迁移 (PVE 管理)

这是工作量最大的部分。

1.  **PVE 列表页 (`src/views/pve/tree/index.vue` 等)**
    *   将 `<a-table>` (Arco) 替换为 Vben 的 `useTable` (Hooks 方式) 或 `<Table>` 组件。
    *   利用 Vben 的 Schema Form 生成搜索栏。
2.  **noVNC 控制台集成**
    *   **难点**：noVNC 需要操作原生 DOM 和 WebSocket。
    *   **实现**：
        1.  新建组件 `src/views/pve/console/VncConsole.vue`。
        2.  移植原 `connectVnc` 逻辑。
        3.  确保 `import RFB from '@novnc/novnc/lib/rfb'` 路径正确（Vben 可能会有别名设置，需在 `vite.config.ts` 中配置 alias）。
        4.  **样式适配**：确保 Canvas 容器高度和宽度能自适应 Vben 的 Content 区域。

### 第四阶段：系统管理与权限

1.  **动态菜单**
    *   原项目通过后端返回路由树。
    *   Vben 支持后端动态路由 (`BackMode`)。
    *   需编写适配器，将 Django 返回的 `component: "view/pve/index"` 格式转换为 Vben 识别的组件导入函数。
2.  **用户与角色**
    *   迁移 `系统管理` -> `用户管理` / `角色管理` 页面。

---

## 4. 关键代码片段参考

### 4.1 package.json (依赖锁定)

```json
{
  "dependencies": {
    "@novnc/novnc": "1.4.0", 
    "ant-design-vue": "^4.x",
    "vue": "^3.x"
  }
}
```

### 4.2 API 定义 (TypeScript 示例)

```typescript
// src/api/pve.ts
import { defHttp } from '/@/utils/http/axios';

enum Api {
  GetVmList = '/pve/vms/',
  VmAction = '/pve/action/',
}

/**
 * 获取虚拟机列表
 */
export const getVmList = (params: VmParams) =>
  defHttp.get<VmListResult>({ url: Api.GetVmList, params });
```

### 4.3 noVNC 组件迁移

```typescript
// VncConsole.vue
import { onMounted, ref, onUnmounted } from 'vue';
import RFB from '@novnc/novnc/lib/rfb'; // 确保路径解析正确

const container = ref<HTMLElement | null>(null);
let rfb: any = null;

const connect = (url: string, password: string) => {
  if (!container.value) return;
  
  rfb = new RFB(container.value, url, {
    credentials: { password }
  });
  
  rfb.addEventListener("connect", () => {
    console.log("Connected to VNC");
  });
};
// ...
```

## 5. 风险与注意事项

1.  **WebSocket 转发**：
    *   开发环境：Vite Proxy 必须开启 `ws: true`。
    *   生产环境：Nginx 必须配置 `Upgrade` 头以支持 WebSocket。
2.  **TypeScript 类型地狱**：
    *   初期为了进度，遇到复杂类型可先用 `any`，后期逐步补全，避免卡在类型报错上。
3.  **样式污染**：
    *   由于 Vben 封装度高，尽量使用它的 `style` 变量和 TailwindCSS (如果开启) 类名，减少手写 Scoped CSS。

---

## 6. 后续计划

1.  执行 `pnpm create vben@latest`。
2.  提交初始版本代码。
3.  按模块优先级进行迁移：登录 -> 布局 -> PVE 核心 -> 系统管理。
