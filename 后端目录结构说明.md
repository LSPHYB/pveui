# 后端代码目录结构说明

本文档详细说明了 `backend/` 目录下的代码结构。项目基于 **Django + Django REST Framework** 开发，集成了 ASGI (Daphne) 以支持 WebSocket。

## 根目录结构

| 文件/目录 | 说明 |
| :--- | :--- |
| `manage.py` | **Django 管理脚本**，用于运行服务器、数据库迁移等。 |
| `pveui/` (或 `django_vue_adminx/`) | **项目配置目录**，包含 `settings.py`, `urls.py`, `asgi.py` 等。 |
| `apps/` | **业务应用目录**，存放所有的 Django App。 |
| `run_daphne.sh` | **启动脚本**，用于启动 Daphne ASGI 服务器 (支持 WebSocket)。 |
| `requirements.txt` | **依赖列表**，Python 依赖包。 |
| `db.sqlite3` | **数据库文件** (开发环境)。 |

## 核心应用模块 (`apps/`)

### 1. `apps/pve` (Proxmox VE 核心交互)
负责所有与 PVE 集群的交互逻辑。

- `pve_client.py`: **PVE API 客户端核心**。封装了 `requests` 与 PVE API 进行通信（登录、获取节点、虚拟机操作、VNC 代理等）。
- `models.py`: **核心数据模型**。定义了 `PVEServer` (PVE 连接配置), `VirtualMachine`/`LXCContainer` (同步/缓存的实例元数据), 以及 `NetworkTopology` (网络拓扑图数据)。
- `views.py`: **API 视图层**。定义了 `/api/pve/...` 接口，调用 `pve_client` 处理前端请求。
- `consumers.py`: **WebSocket 消费者**。处理如 VNC 远程控制、实时日志流等长连接功能。
- `routing.py`: **WebSocket 路由**。
- `serializers.py`: **数据序列化**。

### 2. `apps/rbac` (权限与用户中心)
负责用户、角色、权限、菜单和部门管理。

- `models.py`:
    - `Menu`: **菜单/路由配置**。前端动态菜单的数据源。
    - `Permission`: **接口权限点**。控制后端 API 访问权限。
    - `Role`: **角色**。关联菜单和权限。
    - `UserRole`: 用户与角色关联。
    - `Organization`: **组织/部门**。树形结构。
    - `UserOrganization`: 用户与组织关联。
- `views.py`: 用户登录、获取用户信息、菜单树接口、权限校验接口。

### 3. `apps/audit` (审计日志)
- `models.py`: 定义操作日志、登录日志等模型。
- `middleware.py`: **审计中间件**。自动拦截请求并记录操作日志。

### 4. `apps/chat` (AI 助手)
- `consumers.py`: **Chat WebSocket**。处理 AI 对话的实时流式传输。
- `views.py`: 历史记录查询等。

### 5. `apps/system` (系统配置)
- 管理系统级别的全局配置（如 Logo、系统名称、备案号等）。

### 6. `apps/tasks` (异步任务)
- 可能集成 Celery 或 Django-Q，用于处理耗时任务（如 PVE 镜像下载、备份任务监控）。

### 7. `apps/common` (公共组件)
- `mixins.py`: 通用视图混合类。
- `utils.py`: 通用工具函数。

## 关键文件深入说明

### `apps/pve/pve_client.py`
这是后端的核心驱动文件，主要方法包括：
- `__init__`: 建立 Session，处理 PVE 认证 (Token/Ticket)。
- `get_nodes`: 获取集群节点。
- `get_vms / get_lxc_containers`: 获取虚拟机列表。
- `create_vnc_proxy`: 创建 VNC WebSocket 隧道。
- `start_vm / stop_vm / ...`: 虚拟机生命周期管理。
- `get_node_rrddata`: 获取监控图表数据。

### `apps/rbac/models.py`
定义了权限系统的核心模型：
- **RBAC 模型**：User -> UserRole -> Role -> Permission / Menu。
- **数据权限**：Role 中包含 `data_scope` (如果不只是功能权限，还控制数据可见范围)。

### `run_daphne.sh`
由于项目包含 WebSocket 功能 (VNC, Chat)，不能仅使用 `python manage.py runserver` (WSGI)，必须使用 **Daphne** (ASGI) 启动。
脚本中设置了 `AUTOBAHN_USE_UVLOOP=0` 和 `TWISTED_REACTOR=asyncio` 来解决某些环境下的兼容性问题。
