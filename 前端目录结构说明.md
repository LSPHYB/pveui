# 前端源码详细目录结构说明

本文档完整列出了 `src` 目录下所有的 `.js` 脚本和 `.vue` 组件文件，并对其功能进行了标注。

## 目录树概览

### 1. 根目录与入口
| 文件路径 | 类型 | 说明 |
| :--- | :--- | :--- |
| `src/main.js` | JS | **项目入口文件**，初始化 Vue 实例，引入全局插件（Store, Router, ElementPlus 等）。 |
| `src/App.vue` | Vue | **应用根组件**，仅包含 `<router-view>`，是所有页面的父容器。 |
| `src/permission.js` | JS | **全局权限控制**，定义路由守卫 (beforeEach)，处理登录拦截、Token 校验和动态路由加载。 |

### 2. API 接口层 (`src/api/`)
对应后端的各个 Controller 模块，负责 HTTP 请求封装。

- `audit.js`: **审计日志服务** (获取登录日志、操作日志等)
- `chat.js`: **AI 对话服务** (发送提示词、获取回复)
- `curdexample.js`: **示例服务** (增删改查演示接口)
- `menu.js`: **菜单服务** (获取后端定义的动态菜单树)
- `organization.js`: **组织架构服务** (部门/组织管理)
- `permission.js`: **权限服务** (检查用户权限点)
- `pve.js`: **PVE 核心服务** (Proxmox VE 节点、存储、虚拟机管理接口)
- `role.js`: **角色管理服务**
- `system-setting.js`: **系统配置服务**
- `system.js`: **系统基础服务**
- `task.js`: **任务及其调度服务**
- `upload.js`: **文件上传服务**
- `user-management.js`: **用户管理服务** (后台管理视角)
- `user-organization.js`: **用户-组织关联服务**
- `user-role.js`: **用户-角色关联服务**
- `user.js`: **当前用户服务** (登录、个人信息、登出)

### 3. 组件与布局 (`src/components/`, `src/layout/`)

#### 公共组件
- `components/DataTable/index.vue`: **通用数据表格组件** (封装了 ElementPlus Table，支持分页、自适应高度等)

#### 布局系统 (`layout/`)
负责页面的整体框架结构。
- `layout/content/index.vue`: **内容区容器**，包含 `<router-view>` 和页面切换动画。
- `layout/navbar/index.vue`: **顶部导航栏**，包含面包屑、用户信息、退出登录等。
- `layout/sidebar/index.vue`: **侧边栏容器**，包含 Logo 和菜单滚动区。
- `layout/sidebar/sidebar-item/index.vue`: **递归菜单项组件**，用于渲染多级嵌套菜单。

### 4. 路由与状态 (`src/router/`, `src/store/`)

- `router/index.js`: **路由配置**，定义静态路由（Login, 404）和路由拦截器基础配置。
- `store/index.js`: **状态管理入口** (Pinia/Vuex 实例)。
- `store/modules/menu.js`: **菜单状态**，存储从后端获取的菜单树。
- `store/modules/user.js`: **用户状态**，存储 Token、用户信息、登录状态。

### 5. 工具库 (`src/utils/`)

- `auth.js`: **认证工具**，封装 Token 的 Cookie 读写操作。
- `component-loader.js`: **动态组件加载器**，解决 Vite 中动态 import 路径的问题，用于将后端返回的组件字符串转换为实际组件。
- `menu.js`: **菜单处理工具**，核心逻辑 `menuTreeToRoutes`，将后端菜单树转换为 Vue Router 路由配置。
- `permission.js`: **权限校验工具**，辅助函数 `checkPermission`。
- `request.js`: **HTTP 请求工具**，Axios 的二次封装，包含请求/响应拦截器。

### 6. 钩子函数 (`src/hooks/`)
- `loading.js`: **加载状态钩子**，提供通用的 loading 变量和切换函数。

### 7. 页面视图 (`src/views/`)
实际的业务页面文件。

#### 基础页面
- `view/login/index.vue`: **登录页**
- `view/dashboard/index.vue`: **仪表盘/首页**
- `view/404.vue`: **404 错误页**
- `view/menu-page/index.vue`: **空白/默认菜单页** (当后端菜单未指定组件时的兜底页面)

#### PVE 管理模块 (`views/pve/`)
- `pve/server/index.vue`: **服务器/节点列表**
- `pve/storage/index.vue`: **存储管理列表**
- `pve/network/index.vue`: **网络管理**
- `pve/tasks/index.vue`: **任务日志**
- `pve/templates/index.vue`: **模板管理**
- `pve/node-monitor/index.vue`: **节点监控详情**
- `pve/topology/index.vue`: **网络拓扑图查看**
- `pve/topology/edit.vue`: **拓扑图编辑**

**虚拟机 (VM) / 容器 (LXC) 相关**
- `pve/lxc/index.vue`: **LXC 容器列表**
- `pve/vm/index.vue`: **虚拟机列表**
- `pve/vm/detail.vue`: **虚拟机详情页容器**
- `pve/vm/components/`: **虚拟机详情子组件 (Tab页)**
  - `VMOverviewTab.vue`: **概览** (CPU/内存使用率)
  - `VMConsoleTab.vue`: **控制台** (VNC/Spice)
  - `VMHardwareTab.vue`: **硬件配置**
  - `VMOptionsTab.vue`: **选项设置**
  - `VMTasksTab.vue`: **任务记录**
  - `VMBackupTab.vue`: **备份管理**
  - `VMSnapshotTab.vue`: **快照管理**
  - `VMConfigTab.vue`: **配置文件查看**

#### 系统管理模块 (`views/system/`)
- `system/user/index.vue`: **用户管理**
- `system/role/index.vue`: **角色管理**
- `system/menu/index.vue`: **菜单/权限管理**
- `system/organization/index.vue`: **组织架构管理**
- `system/setting/index.vue`: **系统参数设置** (如 Logo、标题)
- `system/monitor/index.vue`: **系统监控**
- `system/profile/index.vue`: **个人中心**
- `system/permission/index.vue`: **权限点管理** (开发用)
- `system/task/index.vue`: **定时任务管理**
- `system/login-log/index.vue`: **登录日志**
- `system/operation-log/index.vue`: **操作日志**

#### 其他模块
- `views/chat/index.vue`: **AI 助手对话页面**
- `views/curdexample/index.vue`: **增删改查示例页面**
- `views/table/index.vue`: **复杂表格示例**
